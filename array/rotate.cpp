#include <iostream>
using namespace std;
    void rotate(vector<int>& nums, int k) {
        int count = 1, start = 0, end ;

        while(count != k+1){
            nums[start] = nums[(start+1)]
        }

    }

int main(){

    return 0;
}