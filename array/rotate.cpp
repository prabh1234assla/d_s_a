#include <iostream>
using namespace std;
    void rotate(vector<int>& nums, int k) {
        int count = 1, start = 0, temp = nums[start];

        while(count <= k){
            nums[start] = nums[(start+1)]
        }

    }

int main(){

    return 0;
}